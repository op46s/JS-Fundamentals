function personInfo(fName, lName, age) {

    let person = {
        // firstName: fName,
        // lastName: lName,
        // age: age
    }
    person.firstName = fName;
    person.lastName = lName;
    person.age = age
   console.log(person);
}
personInfo("Peter", 
"Pan",
"20"
)
